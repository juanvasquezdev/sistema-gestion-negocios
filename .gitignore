# Editor / OS
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
.DS_Store
Thumbs.db

# Environment (por si algún día hay .env a nivel raíz)
.env
.env.local

# Frontend (se activa cuando exista frontend/)
frontend/node_modules
frontend/.next
frontend/.env*